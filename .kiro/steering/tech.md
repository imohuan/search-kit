# 技术栈与构建系统

## 核心框架
- **Vue 3** (^3.5.24)：渐进式 JavaScript 框架，使用 Composition API
- **TypeScript** (~5.9.3)：JavaScript 的静态类型检查
- **Vite** (8.0.0-beta.0)：下一代前端构建工具，支持 HMR

## 状态管理与路由
- **Pinia** (^3.0.4)：Vue 3 官方状态管理库
- **Vue Router** (^4.6.3)：Vue.js 官方路由库

## UI 与样式
- **Tailwind CSS** (^4.1.17)：功能优先的 CSS 框架
- **@tailwindcss/vite** (^4.1.17)：Tailwind 的 Vite 插件
- **@vicons/material** (^0.13.0)：Material Design 图标库
- **vfonts** (^0.0.3)：图标库字体包

## 文档处理
- **mammoth** (^1.11.0)：DOCX 转 HTML 转换器
- **pdfjs-dist** (^5.4.530)：PDF.js 用于 PDF 文本提取

## 数据库与存储
- **Dexie** (^4.2.1)：IndexedDB 的高级 API 封装

## 工具库
- **lodash-es** (^4.17.21)：实用函数库（支持 Tree-shaking）
- **@vueuse/core** (^14.1.0)：Vue 3 组合式工具集
- **mitt** (^3.0.1)：轻量级事件发射器

## 代码质量
- **oxlint** (^1.31.0)：快速 JavaScript 代码检查工具（基于 Rust）
- **oxfmt** (^0.16.0)：快速代码格式化工具（基于 Rust）

## 测试
- **Vitest** (^4.0.17)：Vite 原生单元测试框架
- **@vitest/coverage-v8** (^4.0.17)：代码覆盖率报告
- **jsdom** (^27.4.0)：Node.js 中的 DOM 实现
- **fake-indexeddb** (^6.2.5)：IndexedDB 模拟库
- **fast-check** (^4.5.3)：属性测试库

## 常用命令

### 开发
```bash
pnpm run dev          # 启动开发服务器，支持 HMR (http://localhost:5173)
pnpm run preview      # 本地预览生产构建
```

### 构建
```bash
pnpm run build        # 类型检查 + Vite 构建
```

### 代码质量
```bash
pnpm run lint         # 运行 oxlint 检查 src 目录
pnpm run format       # 使用 oxfmt 格式化代码
pnpm run lint:format  # 同时运行 lint 和 format
```

### 测试
```bash
pnpm run test         # 运行一次测试
pnpm run test:watch   # 监听模式运行测试
pnpm run test:coverage # 生成覆盖率报告
```

### CI 流程
```bash
pnpm run ci           # 完整 CI：lint → format → build
```

## 构建配置
- **路径别名**：`@` 指向 `./src`
- **CSS 最小化**：使用 LightningCSS（基于 Rust，比 esbuild 更快）
- **服务器预热**：预缓存 `main.ts` 和 `App.vue` 以加快开发启动
- **目标**：ES2020+（Vite 默认）

## 环境要求
- **包管理器**：pnpm（使用 Vite 8.0.0-beta.0 覆盖）
- **Node 版本**：18+ 推荐
- **平台**：Windows PowerShell（主要）、跨平台兼容

## 📚 重要提示

**Tailwind CSS v4 注意事项：**
- 在 Vue 组件的 `<style scoped>` 中使用 `@apply` 时，需要在开头添加 `@reference "@/style.css";`
- 详见 `docs/开发问题记录.md` 中的问题 1
